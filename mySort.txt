
The Radix sort is a linear algorithm that compares elements digit by digit
Time Complexity: O(n)
Space Complexity: O(n)

Pseudo Code:
//step1: get the highest value of the array
//step 2: do counting sort for all digits
//step 3: sort array based on the placement of different digits

counting sort:
for(int i = 0; i< list.length - 1; i ++)
//store the number of elements in count array

for(int i = 0; i < 10; i++)
//change count[i] to the actual position of the output array

for(int i = 0; i>= 0; i--)
//create the output array

for(i = 0; i < n; i++)
//copy the output[] to the original array

